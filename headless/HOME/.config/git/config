[core]
        editor = emacs --no-window-system
        pager = less
[alias]
        statistics = shortlog --summary --numbered --all
        contributors = ! git log --format='%aN' | sort --unique
[include]
        path = ext_config
        path = mail_config
[pull]
        ff = only
[init]
        defaultBranch = master
